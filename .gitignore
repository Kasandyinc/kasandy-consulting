# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# env files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# vercel
.vercel

# prospect workbooks — contain real prospect PII (names/emails); keep local, never commit.
# The engine:import script reads them from /data at import time.
/data/*.xlsx
# Generated prospect seed SQL also contains PII — never commit.
/supabase/seed/*.sql

# typescript
*.tsbuildinfo
next-env.d.ts
.env.local
.env*.local
